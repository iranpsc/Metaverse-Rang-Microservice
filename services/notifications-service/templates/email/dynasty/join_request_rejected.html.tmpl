{{define "email/dynasty/join_request_rejected"}}
{{template "base" .}}
{{end}}

{{define "email/dynasty/join_request_rejected/content"}}
  <h1>درخواست پیوستن رد شد</h1>
  {{if or (eq .Side "receiver") (eq .Side "received")}}
  <p>
    {{with .RecipientName}}سلام {{.}}،{{else}}سلام شهروند عزیز،{{end}}
    درخواست پیوستن به خاندان {{with .DynastyName}}{{.}}{{else}}منتخب{{end}} را رد کردی.
  </p>
  {{else}}
  <p>
    {{with .RecipientName}}سلام {{.}}،{{else}}سلام شهروند عزیز،{{end}}
    متأسفانه درخواست پیوستن به خاندان {{with .DynastyName}}{{.}}{{else}}تو{{end}} تأیید نشد.
    می‌توانی پس از بررسی دلایل احتمالی، درخواست تازه‌ای ثبت کنی.
  </p>
  {{end}}

  <table class="details-table" role="presentation">
    <tbody>
      <tr>
        <th>نام خاندان</th>
        <td>{{with .DynastyName}}{{.}}{{else}}—{{end}}</td>
      </tr>
      {{with .DynastyCode}}
      <tr>
        <th>شناسه خاندان</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      {{with .RejectedAt}}
      <tr>
        <th>زمان رد</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
      {{with .RejectionReason}}
      <tr>
        <th>توضیح</th>
        <td>{{.}}</td>
      </tr>
      {{end}}
    </tbody>
  </table>

  {{if or (eq .Side "receiver") (eq .Side "received")}}
  <p>
    در صورت نیاز می‌توانی وضعیت خاندان را از پنل دنبال کنی.
  </p>
  {{else}}
  <p>
    برای افزایش شانس پذیرش، پروفایل خود را کامل کن و اهداف مشارکتت را شفاف بنویس.
  </p>
  {{end}}

  <div style="text-align:center; margin:24px 0;">
    {{with .ExploreURL}}
      <a href="{{.}}" class="button">جستجوی خاندان‌ها</a>
    {{end}}
    {{with .ProfileURL}}
      <a href="{{.}}" class="button" style="background-color:#3d1554; border:2px solid #fd7e14;">ویرایش پروفایل</a>
    {{end}}
  </div>

  <p class="muted">
    متارنگ همیشه پذیرای ایده‌های تازه است؛ ناامید نشو و دوباره تلاش کن.
  </p>
{{end}}
